### Ansible Managed File ###

[mysqld]
bind-address            = {{ controller1_mgmt_ip }}
default-storage-engine  = innodb
innodb_file_per_table
max_connections         = 4096
collation-server        = utf8_general_ci
character-set-server    = utf8